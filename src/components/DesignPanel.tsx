import { Section } from "@radix-ui/themes";

const DesignPanel = () => {
  return (
    <Section className="fixed h-dvh p-8 right-0">
      <div className="bg-[#2c2c2c] rounded h-full">DesignPanel</div>
    </Section>
  );
};

export default DesignPanel;
